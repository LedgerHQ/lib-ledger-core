
ErrorCode = enum {
    # Device errors

    # Wallet errors

    # User land errors
    cancelled_by_user;


    # Others
    invalid_base58_format;
    invalid_checksum;
    invalid_version;

    unknown;
}

Error = record {
    code: ErrorCode;
    message: string;
}